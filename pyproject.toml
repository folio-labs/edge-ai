[project]
name = "folio-edge-ai"
version = "0.3.0"
description = "Edge FOLIO Module for AI interactions"
license = "Apache2"
readme = "README.md"

requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.11",
    "folioclient>=0.61.3",
    "pydantic-ai>=0.0.41",
]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "pytest-cov>=6.0.0",
    "ruff>=0.11.0",
]
